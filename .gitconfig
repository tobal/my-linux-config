[user]
    name = Tobal
    email = tobal17@gmail.com

[core]
    editor = vim

[alias]
    discard = checkout --
    unstage = reset HEAD --
    stash-unapply = !git stash show -p | git apply -R

[color]
    ui = auto

[push]
    #default = upstream
    default = tracking

[grep]
    linenumber = true
